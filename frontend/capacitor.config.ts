import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mishrasons.landguardai',
  appName: 'LANDGUARD-AI',
  webDir: 'dist'
};

export default config;
